# Laravel PostgreSQL Backup

This package owns generic PostgreSQL schema generation, export, and import. Keep application hosts, credentials, TimescaleDB table rules, migration archival, and scheduling in consuming applications. Use `just check` for changes. Tests must not contact a live database. `just release` publishes a tag and GitHub release; run it only when release is authorized.
