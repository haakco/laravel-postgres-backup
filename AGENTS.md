# Laravel PostgreSQL Backup

This package owns generic PostgreSQL schema generation, export, and import. Keep application hosts, credentials, TimescaleDB table rules, migration archival, and scheduling in consuming applications. Use `composer test` and `composer format:check` for changes. Tests must not contact a live database.
